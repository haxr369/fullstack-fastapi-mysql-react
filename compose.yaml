services:

  backend:
    build:
      context: .
      target: builder
    container_name: livinglab-APIs
    volumes:
      - ./APIs:/home/APIs
    secrets:
      - db-password
    networks:
      - react-fastapi
      - fastapi-mysql
    environment:
      PORT: 8000
      MYSQL_HOST: '172.20.0.3'
    ports:
      - '8002:8000'
    depends_on:
      db:
        condition: service_healthy

  db:
    # We use a mariadb image which supports both amd64 & arm64 architecture
    #image: mariadb:10.6.4-focal
    # If you really want to use MySQL, uncomment the following line
    image: mysql:8.0.19
    container_name: livinglab-db
    environment:
      - MYSQL_DATABASE=example
      - MYSQL_ROOT_PASSWORD_FILE=/run/secrets/db-password
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "--silent"]
      interval: 3s
      retries: 5
      start_period: 30s
    secrets:
      - db-password
    volumes:
      - db-data:/home/solsol/prj/multi-docker/react-fastapi-mysql
    networks:
      - fastapi-mysql

  frontend:
    build:
      context: frontend
      target: development
    container_name: livinglab-frontend
    ports:
      - 3005:3000
    volumes:
      - ./frontend/src:/code/src
      - /project/node_modules
    networks:
      - react-fastapi
    depends_on:
      - backend
    expose:
      - 3308
      - 33080

volumes:
  db-data: {}
secrets:
  db-password:
    file: db/password.txt

networks:
  react-fastapi: {}
  fastapi-mysql: {}
